<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".View.MapActivity.MapActivity">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/map"
        tools:context=".MapsActivity"
        android:name="com.google.android.gms.maps.SupportMapFragment" />
    
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_margin="10dp"
        android:elevation="10dp"
        android:background="@drawable/white_border"
        android:layout_above="@+id/dest_search_layout"
        android:id="@+id/origin_search_layout">

        <ImageView android:id="@+id/origin_search_image"
            style="@style/Location_Search_Image_Button"/>

        <EditText android:id="@+id/origin_search_input"
            android:layout_toEndOf="@id/origin_search_image"
            style="@style/Location_Search_Text_Button"
            android:hint="Enter Origin Address, City, ZipCode"/>
    </RelativeLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_margin="10dp"
        android:elevation="10dp"
        android:background="@drawable/white_border"
        android:layout_alignParentBottom="true"
        android:id="@id/dest_search_layout">

        <ImageView android:id="@+id/dest_search_image"
            style="@style/Location_Search_Image_Button"/>

        <EditText android:id="@+id/dest_search_input"
            android:layout_toEndOf="@id/dest_search_image"
            style="@style/Location_Search_Text_Button"
            android:hint="Enter Destination Address, City, ZipCode"/>
    </RelativeLayout>

</RelativeLayout>